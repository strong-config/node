{
  "explanation": [
    "FIXME: Find out why oclif acts up without this hack",
    "When running the typescript compiler with the normal build config `tsconfig.build.json`",
    "then creating the oclif manifest file via `yarn oclif-dev manifest` fails with a cryptic error.",
    "oclif seems to fail if the 'src' folder is missing from the typescript output, no idea why."
  ],
  "extends": "./tsconfig.build.json",
  "compilerOptions": {
    "rootDir": "."
  }
}
