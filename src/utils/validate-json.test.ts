describe('validateJson()', () => {
  test.todo('compiles the schema')

  test.todo('validates the config against the schema')

  test.todo('returns true when the validation succeeds')

  test.todo('throws when the validation fails')
})
